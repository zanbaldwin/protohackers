[workspace]
members = [
    "echo",
    "primes",
    "keystore",
    "chat",
    "db",
    "mob",
    "speed",
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
debug = false
